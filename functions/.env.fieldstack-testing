# Local dev - emulator only
# NOTE: FIREBASE_AUTH_EMULATOR_HOST and FIRESTORE_EMULATOR_HOST use reserved
# prefixes and cannot be set here. The Functions emulator sets them automatically.
# If you need them outside the emulator, export them in your shell or .env.local.

# Stripe (not needed for auth/user creation, but prevents startup warnings)
STRIPE_SECRET_KEY=sk_test_placeholder
STRIPE_WEBHOOK_SECRET=whsec_placeholder

# App
APP_NAME=FieldStack
APP_URL=http://localhost:5173
SUPPORT_EMAIL=dev@localhost
EMAIL_FROM=dev@localhost
FRONTEND_URL=http://localhost:5173

# CORS
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173

# Resend (not needed locally)
RESEND_API_KEY=re_placeholder
