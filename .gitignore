node_modules/
dist/
# Ignore compiled TypeScript output in functions, not source lib directories
functions/lib/
.env
.env.local
*.log
.firebase/

# Firebase emulator exports — only keep the fixed emulator-data directory
firebase-export-*/

# Ignore emulator data from version control (contains local dev data only)
emulator-data/
